import * as controller from "./electionConfig.controller";

export const POST = controller.createElectionConfig;
export const GET = controller.getElectionConfig;
export const PUT = controller.updateElectionConfig;
